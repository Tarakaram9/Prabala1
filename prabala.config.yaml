# Prabala Configuration
# ─────────────────────────────────────────────────────────────
baseUrl: "https://demo.playwright.dev/todomvc"
browser: chromium
headless: true
timeout: 30000
objectRepositoryDir: object-repository
testDataDir: test-data
outputDir: artifacts
keywordsDir: keywords
env:
  ENVIRONMENT: "dev"
