{
  "name": "@prabala/driver-sap",
  "version": "0.1.0",
  "description": "Prabala SAP GUI Driver – COM/Scripting API automation for SAP GUI on Windows",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@prabala/core": "*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "rimraf": "^5.0.0"
  },
  "license": "Apache-2.0",
  "note": "Runtime only supported on Windows with SAP GUI 7.x+ and SAP Scripting enabled. winax is a Windows-only optional dependency."
}
